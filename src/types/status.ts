type status = {
    id: string,
    user_id: string,
    type: string,
    img_id: string | null,
    text: string,
    created_date: string,
    expired_at: string,
    time: string
}

export default status